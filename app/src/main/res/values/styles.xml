<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:statusBarColor">@color/colorPrimaryDark</item>
        <item name="android:windowBackground">?android:attr/colorBackground</item>
    </style>

    <style name="ToolbarTheme" parent="ThemeOverlay.AppCompat.Light"/>

    <style name="ToolbarTheme.TabText" parent="TextAppearance.Design.Tab">
        <item name="android:fontFamily">@font/librefranklin_bold</item>
    </style>

    <style name="ToolbarTheme.TitleText" parent="TextAppearance.Widget.AppCompat.Toolbar.Title">
        <item name="android:textSize">20sp</item>
        <item name="android:textColor">@android:color/black</item>
        <item name="android:fontFamily">@font/librefranklin_bold</item>
    </style>

</resources>
